version: '3'

services:
  app:
    build: .
    restart: unless-stopped
    volumes:
      - ./avatars:/usr/src/app/avatars
      - ./data:/usr/src/app/data
    environment:
      - FLASHPAPER_PROXIED=true
    ports:
      - 5000:5000
